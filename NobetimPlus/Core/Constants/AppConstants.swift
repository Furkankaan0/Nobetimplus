import Foundation

enum AppConstants {
    static let displayName = "Nöbetim+"
    static let bundleIdentifier = "com.nobetimplus.app"
    static let appGroupIdentifier = "group.com.nobetimplus.app"
    static let defaultCurrencyCode = "TRY"
}
