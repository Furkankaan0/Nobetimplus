// StatCard.swift
// Növera — Legacy Stat Card Components
// All stat components are now in PremiumCards.swift / GlassCard.swift
// This file is kept for backward compatibility.

import SwiftUI

// All components that were here are now provided by:
// - PremiumMetricCard (via MetricCard typealias in GlassCard.swift)
// - EarningsProgressCard (in GlassCard.swift)
// - PremiumRingProgress (in PremiumCards.swift, wrapped by RingProgress in GlassCard.swift)
