// NoveraButton.swift
// Növera — Legacy Button File (now forwarded to PremiumButtons.swift)
// All button components are now defined in PremiumButtons.swift
// This file is kept for backward compatibility of any remaining references.

import SwiftUI

// All button types are already defined via typealias in DesignTokens.swift:
// NoveraPrimaryButton → PremiumPrimaryButton
// NoveraSecondaryButton → PremiumSecondaryButton
// NoveraGhostButton → PremiumGhostButton
// NoveraIconButton → PremiumIconButton
// NoveraFAB → PremiumFAB
